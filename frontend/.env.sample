# Frontend config — copy this file to .env (Vite exposes only VITE_-prefixed vars to the app).
# These point at the local backend by default; change them if your API runs elsewhere.
VITE_API_URL=http://localhost:8000/api/v1
VITE_SOCKET_URL=http://localhost:8000
